{
  "private": true,
  "scripts": {
    "build:native": "pnpm --filter @electron-texture-bridge/native run build",
    "build:core": "pnpm --filter @electron-texture-bridge/core run build",
    "build": "pnpm run build:native && pnpm run build:core",
    "dev:example": "pnpm --filter @electron-texture-bridge/example run dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild", "electron-winstaller"]
  },
  "devDependencies": {},
  "license": "MIT"
}
